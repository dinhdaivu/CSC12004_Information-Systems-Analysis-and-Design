---
name: draft-report
description: Auto-draft a complete, grounded GitHub issue or PR report for the HomeStay Dorm (CSC12004) repo from a task title/description, then WRITE it to `.claude/drafts/draft-issue.md` for `create-issue`. Picks this repo's template (feature / implementation-task / bug / PR), fills EVERY section grounded in CLAUDE.md + docs/, and ticks scope/acceptance checkboxes by real decision. Use when the user wants to "fill the report", "write the issue", "draft a PR description", or "fill the template".
---

# Draft Report (CSC12004 HomeStay Dorm)

> Generated by `specialize-skill` from the general `draft-report` skill for
> `dinhdaivu/CSC12004_Information-Systems-Analysis-and-Design`. Concrete to THIS
> repo. Re-run `specialize-skill draft-report` after the general skill changes.

Turn a one-line task into a fully-filled issue/PR report using this repo's own
templates, then **write it to `.claude/drafts/draft-issue.md`** so `create-issue`
can file it. The output must be **ready to use** — every `<!-- ... -->` comment
replaced with real content, every reference pointing at a file that exists, every
checkbox decided.

## Inputs

- A task title and/or description (from `$ARGUMENTS` or the conversation).
- Optionally which report ("issue", "PR", "bug") and an output path
  (default `.claude/drafts/draft-issue.md`).

## Step 1 — Pick the template (this repo's templates)

| Title tag / signal | Template | Inferred label |
|---|---|---|
| `[Feature]`, new screen/API/workflow | `.github/ISSUE_TEMPLATE/feature_request.md` | `enhancement` |
| `[Task]` / `[Chore]`, refactor, impl work | `.github/ISSUE_TEMPLATE/implementation-task.md` | `task` |
| `[Bug]`, defect, regression | `.github/ISSUE_TEMPLATE/bug_report.md` | `bug` |
| Describing a change already made / a PR | `.github/pull_request_template.md` | — |

Read the chosen file, strip YAML frontmatter for the body, keep its `title:` as
the suggested title. Honor an explicit user-requested type over auto-detection.

## Step 2 — Gather grounding (do not fabricate)

Cite only files that exist (glob to confirm). This repo's canonical sources:

- `CLAUDE.md` — domain model, BCE entities, actors, business rules.
- `docs/architecture/layers.md`, `api-endpoints.md`, `database.md`, `state-machine.md`.
- `docs/UC/UC1.md`–`UC4.md` (use cases); `docs/SUC/SUC0*.mmd` (system sequence diagrams).
- `docs/tasks/` — task docs named `NN-NN-kebab-slug.md` (e.g. `02-01-checkout-payment-flow.md`).

Then search the repo for keywords from the task title to find the real code
paths (`backend/src/...`, `frontend/src/app/...`) and read the matching files so
*Proposed Behavior* / *Implementation Notes* describe actual current state. For a
PR report, also check `git log --oneline -10` and `git diff --stat`.

## Step 3 — Fill every section

- **Summary / Overview** — 2–5 sentences; the problem and the change, plainly.
- **Business Goal / Why** — tie to the dorm tenant lifecycle, maintainability, or
  course requirements; specific to this task.
- **User / Actor** — from this repo's actors: customer (`user`), sales, manager,
  accountant (`staff`/`admin`). For infra/refactor: "Developers / maintainers
  (no end-user-facing change)".
- **References** — only existing files; clickable relative paths.
- **Scope / Completion checkboxes** — tick what the task actually touches; leave
  the rest unchecked. A backend-only refactor ticks Backend (+ Tests/Docs), not Frontend.
- **Proposed Behavior** — numbered concrete steps (or migration steps for a refactor).
- **Acceptance / Completion** — keep template items, tick those that apply, add
  task-specific ones (e.g. "all endpoints respond identically after the rewrite").
- **Out of Scope / Dependencies** — name adjacent work to exclude; real blockers
  (env vars, migrations, libraries) or "None known".

For **refactor / framework-migration** tasks, call out: source→target stack,
parity requirement, API-contract preservation (`docs/architecture/api-endpoints.md`),
data-layer strategy, test-coverage carryover, rollback/sequencing.

> Note: the chatbot RAG feature exists, but AI work is often scoped OUT of refactor
> tasks — confirm AI in/out of scope per task rather than assuming.

## Step 4 — Write the draft file (contract with `create-issue`)

Write to `.claude/drafts/draft-issue.md` (create the dir if needed) with this exact
structure:

```markdown
---
title: "[Task] <suggested title>"
kind: issue            # issue | pr
template: implementation-task   # which template was used, or "fallback"
labels: [task]         # from the table above; [] if none
---

<the fully-filled report body — no placeholders, no dangling docs/... refs>
```

## Step 5 — Deliver

1. Confirm the file path written.
2. Print the filled report in a fenced ```markdown block for review.
3. Offer follow-ups (don't run without asking): run `create-issue` to file it, or
   copy into `docs/tasks/<NN-NN-slug>.md` (pick the next free `NN-NN-` number).

## Quality bar

- Draft written with valid frontmatter; zero `<!-- ... -->` or `docs/...` placeholders.
- Every referenced path verified to exist.
- Checkboxes reflect a real decision; content lets a teammate start from it alone.
